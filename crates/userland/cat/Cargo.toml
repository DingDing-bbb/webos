[package]
name = "webos-cat"
version = "0.1.0"
edition = "2021"
description = "WebOS cat command - Rust userland program"

[lib]
crate-type = ["cdylib"]

[dependencies]
webos-userland = { path = "../lib" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true

[profile.dev]
panic = "abort"
