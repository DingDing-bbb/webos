[package]
name = "webos-init"
version = "0.1.0"
edition = "2021"
description = "WebOS init process - first userland process"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true

[profile.dev]
panic = "abort"
